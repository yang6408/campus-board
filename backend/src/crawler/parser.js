// TODO: cheerio import

// parseNotices(html) — HTML을 파싱하여 공지사항 배열 반환
// @param {string} html - 크롤링한 원본 HTML
// @returns {Array<{ title, url, source, crawledAt }>}

// TODO: cheerio.load(html)로 $ 로드

// TODO: 대상 사이트의 HTML 구조에 맞게 CSS 셀렉터 작성
// TODO: 각 항목에서 title, href 추출
// TODO: 상대 URL을 절대 URL로 변환 (new URL(href, baseUrl))
// TODO: { title, url, source, crawledAt } 형태로 배열에 push

// TODO: { parseNotices } export
